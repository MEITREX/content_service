mutation ($uuid: UUID!, $tag: String){
    addTagToContent(
        contentId: $uuid
        tagName: $tag
    ) {
        id
        name
        rewardPoints
        workedOn
        tagNames
    }
}