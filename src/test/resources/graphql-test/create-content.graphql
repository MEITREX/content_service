mutation {
    createContent(
        input: {
            name: "New Content"
            rewardPoints: 5
            workedOn: false
        }
    ) {
        id
        name
        rewardPoints
        workedOn
    }
}