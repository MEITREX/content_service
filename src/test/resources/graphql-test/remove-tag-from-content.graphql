mutation ($uuid: UUID!, $tag: String){
    removeTagFromContent(
        contentId: $uuid
        tagName: $tag
    ) {
        id
        name
        rewardPoints
        workedOn
        tagNames
    }
}